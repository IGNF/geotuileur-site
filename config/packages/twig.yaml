twig:
    default_path: "%kernel.project_dir%/templates"
    debug: "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    exception_controller: null
    form_themes: ["bundles/Form/ign_theme.html.twig"]

    globals:
        app_title: "%env(APP_TITLE)%"
        app_url: "%env(APP_URL)%"
        url_base_ignkeycloak: "%env(IAM_URL)%"
